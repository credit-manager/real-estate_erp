name: Repair Backup Module Once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repaired backup module
        run: python -m py_compile routes/backup.py
