# 2TO ERP — خطة تحديثات 2026

> وثيقة بحث وتنفيذ: تم جمع أبحاث سوق ERP لعام 2026 وتحديد البنود، ثم تنفيذ ما يمكن تنفيذه
> داخل قاعدة الكود الحالية، وتوثيق ما يحتاج اعتماداً خارجياً (شهادات/نماذج/اتصال).

## 1) ملخص أبحاث السوق (مصادر: Forrester · ERP Research · مواقعه الجهات الضريبية)

| الاتجاه | المُكتَشف | الأثر على 2TO |
|---|---|---|
| السحابة | %89 من النشرات الجديدة أصبح افتراضياً عبر السحابة | نموذج 2TO يدعم النشر السحابي متعدد المستأجرين (Postgres) والسطح المكتبي |
| الذكاء الاصطناعي/الوكلاء | توقعات 2026 تركز على وكلاء ذكاء اصطناعي داخل ERP | مؤجَّل: يحتاج LLM خارجي |
| التحليلات BI | أكثر المنشورات الكبرى انتشاراً (%55 تقريباً) | ✅ نُفِّذت لوحة تحليل تنفيذية محلية |
| المشترون الجدد | %21 من المشترين الصغار يتحولون من Excel وليسوا من ERP سابق | ✅ قوالب/مخزون/فواتير سريعة موجودة أصلاً |
| شركاء التنفيذ | %87 من التركيبات تتم عبر شركاء | موجود (نموذج الترخيص للشركات) |
| الفوترة الإلكترونية | مصر ETA: إلزام فوق 250,000 ج.م بدءاً من **31/3/2026** وعقوبات؛ السعودية ZATCA | ✅ النظام يدعم التقديم لـ 22 دولة (EGY ETA + SA ZATCA) — يتطلب تفعيل الشهادات |
| المقارنة السعرية | Odoo ~24.9$، Business Central ~80$، NetSuite ~99$ لكل مستخدم/شهر | تسعير 2TO السنوي تنافسي من خلال العرض السنوي |

## 2) مصفوفة الحالة (مُنفَّذ / قائم أصلاً / مؤجَّل)

### ✅ مُنفَّذ حديثاً (الإصدار 2.1.1)
| البند | التفاصيل |
|---|---|
| بيع/تجديد سنوي للشركة | `POST /admin/companies/<id>/sale` (لوحة /admin) — تمديد/إنشاء اشتراك 365 يوم، تغيير الباقة، تسجيل دفعة `LicPayment` ونشاط `sale_recorded` |
| لوحة التحليلات التنفيذية | صفحة `/analytics` + `GET /api/bi/analytics` — إيراد/مصروف/هامش، ذمم مدينة/دائنة، ذمم متأخرة + توزيع العمر، قيمة المخزون، أفضل المنتجات والعملاء، اتجاه 12 شهراً |
| التنبيهات الذكية | `GET /api/bi/alerts` — إعادة طلب (تحت الحد الأدنى)، فواتير متأخرة، عقود إيجار تنتهي خلال 30 يوماً، أوامر شراء معلقة، عروض سعرية منتهية الصلاحية |
| التطبيق القابل للتثبيت (PWA) | `manifest.webmanifest` + `sw.js` (نطاق كامل الجذر عبر `/sw.js`) + أيقونات مقاس 192/512 — إمكانية تثبيت كتطبيق |
| رقم الإصدار | `version.py` (2.1.1) + `GET /api/version` + عرضه في لوحة التحليلات |
| وصول لوحة /admin من التطبيق | زر درع بالشريط العلوي + أول بطاقة في مركز النظام (تبويب جديد، لإظهار دور admin فقط عبر `/api/me` + فحص خادم) |
| النسخ الاحتياطي السحابي | `utils/cloud_backup.py` — دفع النسخة التلقائية (المشفّرة) إلى WebDAV أو S3 متناسق (أمازون/MinIO/R2) بتوقيع AWS V4 عبر stdlib فقط، مع إعدادات/اختبار اتصال في صفحة النسخ الاحتياطي |
| توصيات كميات إعادة الطلب | استهلاك شهري من حركات المخزون (آخر 90 يوماً) → أيام التغطية + كمية الطلب المقترحة (مهلة توريد قابلة للضبط `reorder_lead_days`) — في لوحة التحليلات |
| مؤشرات المشاريع | ميزانية مقابل مصروف، نسبة إنجاز، مراحل متأخرة، ومخاطر (تجاوز ميزانية/موعد) مع تنبيه `project_at_risk` — في لوحة التحليلات |
| علامة تجارية حسب فترة التعاقد | `utils/branding.py` — الشعار الفعّال: لوجو الشركة المالكة افتراضياً، وعند تفعيل التجاوز يظهر لوجو الشركة المشتركة طوال فترة الاشتراك النشط (trial/active/grace حتى `end_date`) ثم يعود تلقائياً للشعار المالك؛ `settings.get("system_logo")` يوزّع النتيجة على كل البرنامج والمطبوعات وPDF؛ قسم علامة تجارية في الإعدادات العامة (رفع ملف ≤1.5MB أو رابط + بطاقة فترة التعاقد) |

### 🟢 قائم أصلاً في النظام (استُكشف عند بدء التنفيذ فلا حاجة لإعادة بنائه)
| البند | الموقع |
|---|---|
| الفوترة الإلكترونية متعددة الدول | `utils/einvoice.py` لحقن الفواتير في منظومة الدولة (مصر ETA، السعودية ZATCA، تونس TTN، المغرب DGI...) + حقول `einv_status/einv_reference/einv_qr` على الفواتير + مطبوعات QR + إعدادات `einv_*` |
| النسخ الاحتياطي المشفَّر | `routes/backup.py` — تصدير/استيراد AES-256-GCM (PBKDF2 200k)، **مجدول تلقائي** احتياطي (يومي/غيره + عدد النسخ المحفوظة) + صفحة إعدادات + `/api/backup/health` |
| الفوترة ودفع الإيجار للشركات | `routes/billing.py` + بوابة `billing_portal` |

### ⏸️ مؤجَّل (يتطلب اعتماداً خارجياً أو نماذج غير متوفرة)
| البند | السبب | خطوة لاحقة |
|---|---|---|
| تفعيل الإرسال الفعلي ETA/ZATCA | يحتاج شهادة x.509 صادرة من كل منظومة + بيانات اعتماد + بيئة اختبار حكومية لكل شركة | إدخال الشهادات من إعدادات الفوترة الإلكترونية ثم الربط التلقائي عند اعتماد الفاتورة |
| OCR لمسح الفواتير الورقية | يحتاج نموذج تعرّف ضوئي (ML) خارجي | توثيق بند في خطة النسخ اللاحقة |
| مساعد ذكي عربي (Copilot) | يحتاج LLM خارجي | استكشاف موفّر متوافق مع الخصوصية لاحقاً |
| وحدات الامتثال الخليجي المتقدمة | تفاصيل ZATCA الفاتورة الثانية/الجهات المعتمدة تتطلب مراجعة متخصصة | مستقبلية |

## 3) مخطط التنفيذ المقترح للتحديثات القادمة
1. **الفوترة الإلكترونية الحية** — تشغيل التقديم التجريبي لمنظومة ETA (مرحلة التشغيل التجريبي) بمجرد توفر الشهادات. *(معلَّق بانتظار شهادات x.509)*
2. ✅ **النسخ الاحتياطي السحابي** — دفع النسخة المشفرة إلى تخزين خارجي اختيارياً (WebDAV / S3-متناسق) — نُفِّذ في `utils/cloud_backup.py`.
3. ✅ **توصيات إعادة الطلب الذكية** — اقتراح كميات بناءً على معدل الاستهلاك الشهري من حركات المخزون — نُفِّذ في لوحة التحليلات.
4. ✅ **مؤشرات مشاريع/مقاولات** داخل لوحة التحليلات (الموازنة/المصروف/الإنجاز/المخاطر) — نُفِّذت.
5. **OCR فواتير الشراء** عبر نموذج محلي صغير يعمل داخل الجهاز (خصوصية) — مؤجَّل حتى توفر نموذج OCR خفيف مجاني.
6. ✅ **العلامة التجارية حسب فترة التعاقد** — لوجو الشركة المالكة افتراضياً، ولوجو الشركة المشتركة يظهر تلقائياً طوال الاشتراك النشط (trial/active/grace حتى `end_date`) ثم يعود شعار المالك بعد الانتهاء — نُفِّذت في `utils/branding.py` + قسم «العلامة التجارية» في الإعدادات العامة.